# Database Connection (Neon / PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
DATABASE_URL="postgres://postgres:devpass@localhost:5432/rms_dev?sslmode=disable"

# Application Port (default: 8080)
PORT=8080

# Environment Mode: debug | release
GIN_MODE=debug
